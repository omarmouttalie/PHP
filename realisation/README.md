# Simple PHP Quiz App

## Requirements
- PHP 7+ installed locally
- No database required
- Runs entirely with PHP files

## Setup
1. Clone or copy the files into a folder `quiz-app`.
2. Start a local server:
   ```bash
   php -S localhost:8000
